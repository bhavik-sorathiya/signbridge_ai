name: singlanguage12
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Cupertino icons
  cupertino_icons: ^1.0.8

  # External packages required by your main.dart
  camera: ^0.11.2
  google_fonts: ^5.1.0
  video_player: ^2.6.1
  path_provider: ^2.0.15
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
